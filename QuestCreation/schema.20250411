questSystems:
    type: object
    properties:
      name: string
    subcollections:
      Example System Quests:
          type: object
          properties:
            questRank: string
            questPrompt: string
            questEventCount: string
            questAuraGranted: string
            questRepeatDebuff: string
            questName: string
            questEventUnits: string
    type: object
    properties:
      name: string
    subcollections:
      Example System Quests:
users:
    type: object
    properties:
      aura: integer
      agility:
        type: object
        properties:
          speed: integer
          balance: integer
          flexibility: integer
      skillPoints: string
      id: string
      strength:
        type: object
        properties:
          core: integer
          lowerBody: integer
          upperBody: integer
      focus: integer
      power: integer
      initiative: integer
      discipline: integer
      stamina: integer
      email: string
      name: string
